#include "holberton.h"
/**
* *_strchr - locates a character in a string
* @s: string
* @c: character
* Return: c in string, NULL if not found
*/
char *_strchr(char *s, char c);
{
	return (c);
	return (NULL);
}
